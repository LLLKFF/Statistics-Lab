# Data

本目录只保存可以合法公开、来源清晰且适合进入版本管理的数据或说明文件。

## 必须记录

- 数据来源与获取时间；
- 样本范围、频率与版本；
- 字段定义和单位；
- 清洗、合并与排除规则；
- 许可、引用和再分发限制；
- 原始数据到分析数据的可重复处理步骤。

受许可限制的数据不得提交到仓库，只保存字段说明、获取方式与校验信息。本地 `raw/`、`processed/` 和 `external/` 目录默认被 Git 忽略。
